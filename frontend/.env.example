# Leave empty for same-domain deployments where the backend is mounted at /api.
# For separate frontend/backend deployments, set this to your backend API base URL.
VITE_API_URL=http://localhost:5000/api
